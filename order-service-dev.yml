global:
  message: Customer order dev environment message.
server:
  port: 8082
message: Just a message from order dev.
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/order_dev
    username: Artyomlbch
    password: Artyomlbch213853
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    format-sql: true
